
def group(values, size):
    if values is None or size <= 0 or size is None:
        raise ValueError("Error with data")
    
    grouped_output = []

    # while value in values <= size:
    #     grouped_output.append[value]
    return grouped_output
    
if __name__ == "__main__":
    print(group(["Hi","Hello"], 2))